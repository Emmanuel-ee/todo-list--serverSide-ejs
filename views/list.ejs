<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>To Do List</title>
    <link rel="stylesheet" href="css/styles.css" />
  </head>
  <body>
    <div class="Box" id="heading">
      <h1><%=kindOfDay%></h1>
    </div>

    <div class="Box">
      <%for(let i = 0; i < newListItem.length; i++){%>
      <div class="item">
        <input type="checkbox" />
        <p><%=newListItem[i]%></p>
      </div>
      <%}%>

      <form class="item" action="/" method="post">
        <input type="text" name="newItem" placeholder="Enter Task" autocomplete="off" />
        <button type="submit" name="button">+</button>
      </form>
    </div>

    <!-- <%if(kindOfDay === "sunday" || kindOfDay === "Saturday" ){%>
        <h1 style="color: red;">It's a <%=kindOfDay%>!</h1>
    <%}else{%>
        <h1 style="color: green;">It's a <%=kindOfDay%>!</h1>
    <%}%> -->
  </body>
</html>
